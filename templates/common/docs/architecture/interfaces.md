---
entry_globs: []
contract_globs: []
---

# 对外入口与契约基线

> 项目：{{projectName}}
> 技术栈：{{techStack}}
> 最后更新：{{date}}
> 本文件记录本项目自己的答案，不预设项目类型。未使用的形态写「不适用」，不要删行。
>
> 顶部 frontmatter 是给 `ai-memory gate contract` 用的机器可读路径,填了才生效:
> `entry_globs` 是对外入口代码的路径(如 `[src/api/**, src/pages/**]`),
> `contract_globs` 是契约文件的路径(如 `[docs/api/**]`)。只支持 `**` 与 `*`。
> 下方表格是给人看的说明,两者不一致时以 frontmatter 为准。

## 项目类型

- 对外提供什么：
- 主要调用方（用户、其他系统、其他部署单元）：

## 对外入口形态

一个项目可多种并存。按实际情况填写，未使用的写「不适用」。

| 入口形态 | 是否使用 | 位置或命名约定 | 粒度（一组覆盖什么） |
|---|---|---|---|
| 页面 / 视图 | | | |
| API 分组（如按资源或能力分组） | | | |
| 消息 / 事件 | | | |
| 定时任务 | | | |
| CLI 命令 | | | |
| SDK / 库公开接口 | | | |
| 其他（如 Webhook、文件导出） | | | |

## 契约与验证约定

| 约定项 | 本项目的答案 |
|---|---|
| 契约格式（如 OpenAPI、TS 类型、proto、Markdown 表格） | |
| 契约位置（版本控制中的路径） | |
| 产生方向（先写契约，或由代码生成） | |
| 校验命令（可直接执行） | |
| 调用方如何获取类型或客户端 | |
| 契约变更的兼容策略 | |

## 盘点记录

- 盘点方式（已有项目盘点现状 / 0→1 项目确定）：
- 盘点日期：
- 重新盘点触发条件：技术栈变化，或契约格式、位置、产生方向发生变化

## 假设与开放问题

- （每项附验证方式或复审条件）
